source "http://rubygems.org"
 
group :development do
  gem 'guard'
  gem 'ruby_gntp'

  # notifications are os specific
  # TODO make this more explicit
  gem 'growl', :require => false
  gem 'ruby_gntp', :require => false
  gem 'libnotify', :require => false
end
